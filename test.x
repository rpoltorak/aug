print(ala);print;

l := length(zmienna);
print(length("Komputer"));
print;
y := -a;

x := -1000 + 999;
print(x); print;
a := readint;
t := readstr;
if (a = 100 or a = 200) and a <= 500 then a := 1 else a := -1;
if a = false then
begin
	a := 200;
	while a > 0 do a := a - 1
end else 
begin
	t := readstr;
	a := -100 + 100;
	do
		b := readint
	while b <> 0
end;
a := a + 1
